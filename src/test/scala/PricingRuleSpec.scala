import org.scalatest.{BeforeAndAfter, FunSpec}

class PricingRuleSpec extends FunSpec with BeforeAndAfter{
  describe("A Pricing Rule") {
    it("should reference a unique item")(pending)

    it("should have a field 'quantity' that represents the quantity of items before this rule applies")(pending)

    it("should have a field 'price' that represents the new price to apply for this rule")(pending)
  }
}
