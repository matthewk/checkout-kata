import org.scalatest.FunSpec

class BasketSpec extends FunSpec {
  it("should have a field 'items' to store items selected by the user")(pending)

  it("should have a function to add an item")(pending)

  it("should have a function to add a quantity of an item")(pending)

  it("should have a function to add several items with quantities")(pending)

  it("should provide a readable overview of its contents")(pending)
}
