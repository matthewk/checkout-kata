import org.scalatest.{BeforeAndAfter, FunSpec}

class ItemSpec extends FunSpec with BeforeAndAfter {
  describe("An Item") {
    it("should have a field of 'SKU' as a unique identifier")(pending)
    it("should have a single unique character as an 'SKU'")(pending)
    it("should have a field of 'price' to store its individual price")(pending)
  }
}
